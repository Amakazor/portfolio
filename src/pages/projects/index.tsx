import React from 'react';
import {PageProps} from 'gatsby';
import Layout from '../../components/layout';

const IndexPage = (props: PageProps) => {
    return <Layout pageProps={props}>TEST</Layout>;
};

export default IndexPage;
